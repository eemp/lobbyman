{
    "name": "lobbyman",
    "version": "1.0.0",
    "description": "package for creating workers templates",
    "main": "index.js",
    "scripts": {
        "dev": "npx wrangler preview --watch",
        "release": "npx wrangler publish --env production",
        "test": "echo \"Error: no test specified\" && exit 1",
        "format": "prettier --write '**/*.{js,css,json,md}'"
    },
    "author": "Mihir Patel <ee.mihir.patel@gmail.com>",
    "license": "ISC",
    "devDependencies": {
        "@cloudflare/wrangler": "^1.18.0",
        "prettier": "^2.0.5"
    },
    "dependencies": {
        "lodash": "^4.17.20",
        "serverless-cloudflare-workers": "^1.2.0",
        "url-pattern": "^1.0.3"
    }
}
